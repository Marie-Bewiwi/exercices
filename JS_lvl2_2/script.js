function addition (nombre) { // ici, nombre est un paramètre : il est voué à être remplacé quand la fonction est appelée
   return nombre+nombre
}
console.log (addition(3)) // on définit ici la valeur du paramètre défini précédemment
console.log (addition(5))